import React from 'react';
import { shallow } from 'enzyme';
import PhotoAlbum from './PhotoAlbum';

describe('Photo album tests', () => {
  it('describes a photo album', () => {
    const wrapper = ('')
  })
})
